{
  "name": "generate_skills",
  "description": "Generate a list of technical and soft skills required to deliver a specific Agile work item.",
  "parameters": {
    "type": "object",
    "properties": {
      "skills": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Skills an employee would need to successfully complete the work item."
      }
    },
    "required": ["skills"],
    "additionalProperties": false
  }
}
